[pytest]
testpaths = tests
# required by pytest-homeassistant-custom-component; harmless (and unknown)
# when the suite runs without Home Assistant, e.g. on Windows
asyncio_mode = auto
filterwarnings =
    ignore:Unknown config option
